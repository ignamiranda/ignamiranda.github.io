---
import { getEntry, render } from "astro:content";
import ToolLayout from "../layouts/ToolLayout.astro";

const entry = await getEntry("notes", "battle-map-generator");
if (!entry) {
  throw new Error("Content collection entry 'notes/battle-map-generator' not found");
}
const { Content } = await render(entry);
const slug = entry.slug || "battle-map-generator";
---

<ToolLayout slug={slug}>
  <Content />
</ToolLayout>
