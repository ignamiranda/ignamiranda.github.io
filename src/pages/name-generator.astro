---
import { getEntry, render } from "astro:content";
import ToolLayout from "../layouts/ToolLayout.astro";

const entry = await getEntry("notes", "name-generator");
if (!entry) {
  throw new Error("Content collection entry 'notes/name-generator' not found");
}
const { Content } = await render(entry);
const slug = entry.slug || "name-generator";
---

<ToolLayout slug={slug}>
  <Content />
</ToolLayout>
