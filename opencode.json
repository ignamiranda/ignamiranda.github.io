{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "bash": {
      "*": "allow",
      "gh pr merge *": "deny"
    }
  },
  "command": {
    "env": {
      "description": "Load GH_TOKEN from .env into the current shell",
      "prompt": "Run the PowerShell one-liner to source .env: Get-Content .env | ForEach-Object { $k,$v = $_ -split '=',2; Set-Item \"env:$k\" -Value $v.Trim('\"') }"
    }
  }
}
